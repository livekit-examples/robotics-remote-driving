[project]
name = "livekit-controller"
version = "0.1.0"
description = "LiveKit remote control workspace for Pico driving"
requires-python = ">=3.10"

[tool.uv.workspace]
members = [
    "car-protocol",
    "local-bridge",
    "remote-controller",
    "local-controller",
    "remote-agent",
]
